ktor:
  application:
    modules:
      - com.example.ApplicationKt.module
  deployment:
    port: 8080
jwt:
  audience: "listly-client"
  realm: "Listly API"
  issuer: "listly-backend"
  secret: "super-secret-key"
meilisearch:
  host: "http://localhost:7700"
  apiKey: "masterKey"
  index: "media_items"
